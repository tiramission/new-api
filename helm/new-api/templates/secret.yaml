apiVersion: v1
kind: Secret
metadata:
  name: {{ include "new-api.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "new-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Always wire the primary DSN; switching DB type is handled in helpers.
  SQL_DSN: {{ include "new-api.sqlDsn" . | quote }}
  {{- if .Values.database.logDatabase.enabled }}
  LOG_SQL_DSN: {{ include "new-api.logSqlDsn" . | quote }}
  {{- end }}
  {{- if or .Values.redis.enabled .Values.redis.external.enabled }}
  REDIS_CONN_STRING: {{ include "new-api.redisConnString" . | quote }}
  {{- end }}
  {{- range $k, $v := .Values.extraSecretEnv }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
