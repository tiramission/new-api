apiVersion: v2
name: infra
description: Shared infrastructure (official PostgreSQL + Redis) for new-api deployment. Uses upstream official images mirrored to the Aliyun ACR.
type: application
version: 0.1.0
appVersion: "1.0"
